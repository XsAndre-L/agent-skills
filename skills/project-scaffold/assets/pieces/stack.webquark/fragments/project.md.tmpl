## WebQuark UI foundation

- Shared integration boundary: `{webquark.foundationRoot}`.
- Active integration: `{webquark.integrationEnabled}`.
- Every frontend application reuses this boundary; application-specific WebQuark foundations are not allowed.
- WebQuark base CSS, application token overrides, application layout styles, and isolated public-part exceptions remain separate layers.
