- Route files under `{{backend.root}}/` to the `{{backend.scopeName}}`
  ownership scope. Read the
  [backend boundary](/{{backend.root}}/README.md) before changing
  application/library placement. When present, also read
  `/.agents/{{backend.scopeName}}/memory/rules.md` and
  `/.agents/{{backend.scopeName}}/hooks/workflow.md` for selected framework
  guidance.
