- Route files under `{{frontend.root}}/` to the `{{frontend.scopeName}}`
  ownership scope. Read the
  [frontend boundary](/{{frontend.root}}/README.md) before changing
  application/library placement. When present, also read
  `/.agents/{{frontend.scopeName}}/memory/rules.md` and
  `/.agents/{{frontend.scopeName}}/hooks/workflow.md` for selected framework
  guidance.
