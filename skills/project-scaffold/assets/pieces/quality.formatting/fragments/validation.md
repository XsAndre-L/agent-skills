## Formatting

Run `bun run format:check` when a change touches supported source,
configuration, or documentation. Generated outputs, scaffold provenance, and
vendored formal skills are intentionally excluded.
