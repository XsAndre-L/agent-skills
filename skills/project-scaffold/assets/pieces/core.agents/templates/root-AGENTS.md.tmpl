# {{project.name}} Agent Bootstrap

Before doing repository work:

1. Read `.agents/AGENTS.md` and follow its scope-routing instructions.
2. Use `.agents/README.md` as the knowledge map.
3. Load only the relevant formal skill from `.agents/skills`.

{{contributions:bootstrap.routes}}

Keep this root entry point short. Detailed project knowledge belongs under
`.agents/` so the repository has one source of truth.
