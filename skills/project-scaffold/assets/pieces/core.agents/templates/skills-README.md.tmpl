# Repository Skills

Formal repository skills live in one named directory each and contain a
required `SKILL.md` with matching `name` frontmatter and a `description`.
Add supporting `references/`, `scripts/`, or `assets/` only when the
skill's workflow needs them. Same-name skills are never merged implicitly.

{{contributions:skills.catalog}}
